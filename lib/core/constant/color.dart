import 'package:flutter/material.dart';
//? COLORS
const Color kBlue = Color(0xFF0062BD);
const Color kWhite = Colors.white;
const Color kBlack = Colors.black;
const Color kGrey = Colors.grey;

//?GRADIENT
const LinearGradient kButtonColor = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color(0xff0062bd), Color(0x7f0062bd), Color(0x440062bd)]);

const LinearGradient kBackgroundScreen = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Color(0xd80062bd), Color(0x000062bd)]);
